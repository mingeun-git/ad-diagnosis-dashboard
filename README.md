# 리워드 광고 플랫폼 진단 — 통합 대시보드

데이터 스프린트 팀이 만든 **시스템 3개**를 하나의 이야기로 잇는 포트폴리오 겸 운영 화면이다.
**왼쪽 내비게이션 순서가 곧 이야기 순서**다 —
왜 이 프로젝트인가 → 1차 EDA 신호 3개 → 가설 검증 → 진단·해법 → 시스템 3개 → 마무리.

| # | 시스템 | 담당 | 상태 |
|---|---|---|---|
| ① | 어뷰징 탐지 | 팀원 1 | **완료** — 2026-08-25 자료 수령·검증 후 Ⅱ 화면을 실내용으로 채웠다 |
| ② | 개인화 추천 | 팀원 2 | **자료 대기** — 화면에 자리만 잡아 뒀다 |
| ③ | 광고 선별·진단 | 본인 | **완료** — 실제 산출물이 연결된 살아 있는 화면 |

③은 광고 9,369건을 **제목·설명문만으로 진단**한다.
(BigQuery 모드에서는 시연용 합성 광고 40건이 더해져 9,409건이 보인다.)
LLM 태그 15축 + 텍스트 피처(TF-IDF/SVD)로 성과 이력이 없는 신규 광고에도 기대 CVR 과 등급을 매긴다.
데이터는 BigQuery 에서 실시간으로 읽는다(접속 정보는 Secrets 로 주입).

## 화면 구성 (좌측 내비게이션)
| 그룹 | 메뉴 | 내용 | 데이터 |
|---|---|---|---|
| Ⅰ. 왜 이 프로젝트인가 | 프로젝트 개요 | 한 장 요약 · 플랫폼 구조 · 용어 · 원본 3테이블 | `story.py` |
| | 1차 EDA — 이상 신호 3개 | 데이터·유저·광고 탭 3개 | `story.py` |
| | 가설 검증 | A/B/C군 10개 판정 + 군별 결론 | `story.py` |
| | 진단 → 시스템 3개 | 문제→시스템 매핑 · 의존성 · 상호연결 | `story.py` |
| Ⅱ. 시스템 ① 어뷰징 탐지 | 개요 (팀원 1) | 전처리 vs 탐지 시스템 · 정제 전후 CVR · 규칙 6개 · Isolation Forest · 반복클릭 규칙 3개 · Risk Level · **기각된 신호** · 재계산 검증 · 한계 | `story.ABUSE_*` |
| Ⅲ. 시스템 ② 개인화 추천 | 개요 (팀원 2) | 성립 근거 B1~B5 · **자료 대기 자리** | `story.py` |
| Ⅳ. 시스템 ③ 광고 선별 | 어떻게 만들었나 | 파이프라인 6단계 · 역할분리 실험 · 등급 성능 · 한계 | `story.py` |
| | 재고 진단 | 광고 구성 · 기존 분류축과 비교 | BigQuery |
| | 태그 성과 스코어보드 | 태그별 CVR 순위 | BigQuery |
| | 마찰 분석 ★ | 어떤 참여 조건이 전환을 깎는가 | 파케이 |
| | 광고 카드 | 광고별 기대 CVR · 등급 검색 | BigQuery |
| | 리스크 경보 | 위험 · 고마찰 광고 | BigQuery |
| | 유통 갭 | 매체별 미노출 우수 광고 추천 | BigQuery |
| | 신규 광고 진단 | 성과 이력 없는 신규 광고 (시연용 합성 데이터 포함) | BigQuery |
| | 검증 결과 | 게이트 검증 · 피처 구성 비교 · 모델 비교 | 파케이 |
| Ⅴ. 마무리 | 기술 스택 · 예상 반론 | 스택 · 반론 8개 · 수치 출처 | `story.py` |

## 팀원 자료가 오면 어디를 고치나
`story.py` 의 `PLACEHOLDER_ITEMS` 자리를 실제 내용으로 대체하고,
필요하면 `app.py` 의 `sys_reco` 블록에 표·차트를 추가한다.
화면의 빗금 점선 박스(`krds.placeholder`)는 **완성된 척하지 않기 위한 장치**이므로,
내용이 채워지기 전에는 지우지 말 것.

> **① 어뷰징은 2026-08-25 에 이 절차대로 처리했다.** 팀원 1 의 재현 폴더를 받아
> 전체를 읽고, **핵심 EDA 수치 10개를 우리 원본 데이터로 재계산해 대조**한 뒤
> (9개 정확히 일치 · 1개는 팀원 문서 안에서 값이 갈리던 것을 확정),
> `story.py` 에 `ABUSE_*` 상수 15종을 넣고 `app.py` 의 Ⅱ 화면을 다시 썼다.
> **팀원 시스템의 수치는 우리가 재계산하지 않고 인용한다** — 두 시스템이 서로 다른 GCP
> 프로젝트에 있고, ①은 정제 **전** 원본을, ③은 정제 **후** 마트를 보므로 모집단이 다르다.

## 디자인 — KRDS 적용
화면은 **KRDS(대한민국 정부 디자인 시스템) v1.0.0** 의 규칙을 따른다.

| 파일 | 역할 |
|---|---|
| `.streamlit/config.toml` | 색 · 서체 · 모서리 등 Streamlit 네이티브 테마 (KRDS 토큰 매핑) |
| `krds.py` | 레이아웃 · 컴포넌트 · CSS. **생김새를 고칠 일은 전부 이 파일 하나** |
| `krds_tokens_reference.css` | KRDS 공식 토큰 원본 사본 (출처 확인용, 앱은 읽지 않음) |
| `app.py` | 데이터 로직 + 화면 조립. **CSS 를 쓰지 않는다** |
| `story.py` | 앞단 스토리의 **확정 실측값(하드코딩)**. 원본은 `통합_스토리라인.md` |

- 토큰 출처: <https://github.com/KRDS-uiux/krds-uiux> → `resources/css/token/krds_tokens.css`
- 서체: Pretendard GOV (jsDelivr CDN). 서브셋에 없는 글자는 Pretendard 로 폴백.
- ⚠️ **정부상징 · masthead(정부 누리집 식별 배너)는 쓰지 않았다.** 이 서비스는 정부 서비스가
  아니므로 그대로 쓰면 사칭이 된다. 컬러 · 타이포 · 간격 · 컴포넌트 형태만 차용했다.

## 로컬 실행
```bash
pip install -r requirements.txt
streamlit run app.py
```
로컬에서는 `gcloud auth application-default login` 이 되어 있으면 그대로 붙는다.
`.streamlit/secrets.toml` 이 아예 없으면 로컬 파케이 모드로 떨어지고, 그 파일도 없으면
"산출물이 없습니다" 안내가 뜬다(크래시하지 않는다).

## 배포 (Streamlit Community Cloud)
1. 이 폴더를 GitHub 저장소로 push
2. <https://share.streamlit.io> → New app → 저장소 선택 → `app.py`
3. Advanced settings → Secrets 에 `.streamlit/secrets.toml.example` 내용을 실제 값으로 채워 붙여넣기

> `requirements.txt` 의 `streamlit>=1.50` 핀을 낮추지 말 것.
> `config.toml` 의 `fontFaces` · `baseRadius` · `chartCategoricalColors` 는 1.46+ 에서만 동작한다.

## 데이터에 대한 주의
- **앞단(Ⅰ·Ⅱ·Ⅲ·Ⅴ)의 숫자는 `story.py` 하드코딩**이다. 이미 끝난 분석의 결론이라 재계산하지 않는다.
  원본은 프로젝트 루트 `통합_스토리라인.md` 이며, **문서를 먼저 고치고 `story.py` 를 맞춘다.**
- **`ads_idx >= 990000` 은 시연용 합성 데이터**다. 실제 운영 로그가 아니므로 수치를 인용하지 말 것.
- 신규 광고의 기대 CVR 은 **절대값이 아니라 순위**로 볼 것 (‘신규 광고’ 화면 하단 설명 참조).
- CVR 42.6% 는 **어뷰징 전처리 후** 값이다(원본 8.75%). 외부 인용 시 반드시 병기.
- 이 저장소에는 개인정보가 포함된 컬럼(`user_ip` 등)이 어떤 형태로도 들어 있지 않다.
